#include <stdio.h>

/**
 * main - main block
 * Description: Prints numbers in base10
 * Return: 0
 */

int main(void)
{
	int base10values;

	for (i = 0; i < 10; i++)
	{
		printf("%i", i);
	}
	putchar('\n');

	return (0);
}
