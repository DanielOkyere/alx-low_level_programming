#include "main.h"

/**
 * _abs - _abs function
 * @n: n to provide absolute value
 * Description: Provides Absolute value of number
 * Return: int
 */

int print_sign(int n)
{
	return ((n < 0) ? (n * (-1)) : (n));
}
